export type WorkSpaceRoom = {
    id: number,
    title: string,
    description: string,
    roomType: string,
    pricePerHour: number,
    pricePerDay: number,
    pricePerMonth: number,
    capacity: number,
    area: number, // diện tích

}